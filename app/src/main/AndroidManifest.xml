<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androvote">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Vote"></activity>
        <activity android:name=".VoteIssue" />
        <activity android:name=".CreateIssue" />
        <activity android:name=".ModifyPassword" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GrantPermission" />
        <activity android:name=".UserPage1" />
        <activity android:name=".AddUser" />
        <activity android:name=".Admin_first" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>